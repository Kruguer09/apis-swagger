@model IEnumerable<EuroScaffolding2.Models.EdFestival>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Anyo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ciudad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Anyo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ciudad)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Identificador">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Identificador">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Identificador">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
